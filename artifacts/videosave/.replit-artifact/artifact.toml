kind = "web"
previewPath = "/"
title = "VideoSave"
version = "1.0.0"
id = "artifacts/videosave"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20954

[services.development]
run = "pnpm --filter @workspace/videosave run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/videosave", "run", "build" ]
publicDir = "artifacts/videosave/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20954"
BASE_PATH = "/"
