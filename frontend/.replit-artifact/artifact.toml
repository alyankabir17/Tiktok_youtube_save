kind = "web"
previewPath = "/"
title = "VideoSave"
version = "1.0.0"
id = "frontend"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20954

[services.development]
run = "pnpm --dir backend --filter @workspace/frontend run dev"

[services.production]
build = [ "pnpm", "--dir", "backend", "--filter", "@workspace/frontend", "run", "build" ]
publicDir = "frontend/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20954"
BASE_PATH = "/"
