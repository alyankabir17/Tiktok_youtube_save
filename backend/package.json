{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run typecheck && npm run --if-present build --workspaces",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run --if-present typecheck --workspaces"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
